Multi-line input for reasons, notes and review comments.

```jsx
<Textarea label="เหตุผลการลา" placeholder="ระบุเหตุผล…" rows={4} required />
```

Vertically resizable. Same `label` / `hint` / `error` API as `Input`.
