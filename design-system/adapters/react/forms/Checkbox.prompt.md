Checkbox for opt-ins, multi-select lists and policy acknowledgements.

```jsx
<Checkbox label="ยอมรับนโยบายการลา" checked={ok} onChange={e => setOk(e.target.checked)} />
```

Tick scales in on check. Accepts native input attrs.
