Radio button for single-choice groups (e.g. full-day vs half-day leave). Share a `name` to group.

```jsx
<Radio name="duration" value="full" label="เต็มวัน" defaultChecked />
<Radio name="duration" value="half" label="ครึ่งวัน" />
```
