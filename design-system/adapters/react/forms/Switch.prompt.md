Toggle for settings and preferences (notifications, auto-approve, etc).

```jsx
<Switch label="แจ้งเตือนทางอีเมล" checked={on} onChange={e => setOn(e.target.checked)} />
```

Thumb glides on a brand-filled track when checked.
