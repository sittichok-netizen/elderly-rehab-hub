Dark tooltip on hover/focus — explain icon buttons and truncated values.

```jsx
<Tooltip label="ส่งออกเป็น Excel">
  <IconButton aria-label="ส่งออก"><i data-lucide="download" /></IconButton>
</Tooltip>
```

Appears above the trigger; fades + slides in. Keep labels short.
