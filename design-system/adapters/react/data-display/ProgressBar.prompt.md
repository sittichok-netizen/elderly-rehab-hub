Thin bar for leave-quota usage, goal completion and onboarding progress.

```jsx
<ProgressBar value={6} max={10} />
<ProgressBar value={90} tone="warning" />
```

Tones: `brand` (default) `success` `warning` `danger`. Width animates on change.
