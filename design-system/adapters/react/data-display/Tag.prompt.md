Removable chip for active filters, selected employees and skill/role tags.

```jsx
<Tag onRemove={() => remove(id)}>ฝ่ายขาย</Tag>
<Tag>React</Tag>
```

Pass `onRemove` to show the × button; omit it for a static tag.
